# Enable transparency (requires picom)
background_opacity 0.8

# Font settings
font_family Monospace
font_size 12

# Cursor style
cursor_shape block

# Set colors (Dracula theme as an example)
include ./themes/dracula.conf
